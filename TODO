* create and use ERC-20 contracts through the registry
* audit
* ERC1155LockedERC721
* ERC1155OverERC721
* also wrap ETH
* hook to `yarn export` after `hardhat compile`.
* centralized contract to account all ERC-20 wrappers of ERC-1155
* set app title
