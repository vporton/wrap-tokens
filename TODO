* audit
* Use ERC1155LockedERC721 and ERC1155OverERC721 in UI
* hook to `yarn export` after `hardhat compile`.
* passing addresses as GET params (need to warn they may be malicious)
* Bug when on "ERC20 → ERC1155" entered a wrong symbol in "ERC-1155 token ID"
* More providers than MetaMask
* Reload on network change
* Bottom part of the screen on phone bug.
* Show how transactions are executed.
* Show whether the MetaMask network is supported.

