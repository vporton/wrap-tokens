* use proxy contract to create ERC-20 contracts
* create and use ERC-20 contracts through the registry
* audit
* ERC1155LockedERC721
* ERC1155OverERC721
* also wrap ETH
* hook to `yarn export` after `hardhat compile`.
* passing addresses as GET params (need to warn they may be malicious)
